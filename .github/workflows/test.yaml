name: Test Pipeline

on:
  push:
    branches:
      - dev
      - main
  pull_request:
    branches:
      - dev
      - main

jobs:
  unit:
    runs-on: ubuntu-latest
    environment: Test
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      HCLOUD_TOKEN: ${{ secrets.HCLOUD_TOKEN }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Setup AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-2

      - name: Setup SST
        run: pnpx sst install

      - name: Run unit tests
        run: pnpm run -r test:unit

  integration:
    runs-on: ubuntu-latest
    environment: Test
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
      HCLOUD_TOKEN: ${{ secrets.HCLOUD_TOKEN }}
      USER_DB_USERNAME: ${{ var.USER_DB_USERNAME }}
      USER_DB_PASSWORD: ${{ var.USER_DB_PASSWORD }}
      USER_DB_HOST: ${{ var.USER_DB_HOST }}
      USER_DB_PORT: ${{ var.USER_DB_PORT }}
      USER_DB_DATABASE: ${{ var.USER_DB_DATABASE }}
      MAIN_REDIS_PASSWORD: ${{ var.MAIN_REDIS_PASSWORD }}
      MAIN_REDIS_HOST: ${{ var.MAIN_REDIS_HOST }}
      MAIN_REDIS_PORT: ${{ var.MAIN_REDIS_PORT }}
      PUBLIC_DOMAIN: ${{ var.PUBLIC_DOMAIN }}
      PUBLIC_APP_NAME: ${{ var.PUBLIC_APP_NAME }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install pnpm
        uses: pnpm/action-setup@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Setup AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-2

      - name: Setup SST
        run: pnpx sst install

      - name: Run unit tests
        run: pnpm run -r test:int
