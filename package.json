{
  "name": "homelab-template",
  "version": "0.0.0",
  "packageManager": "pnpm@9.9.0",
  "scripts": {
    "init": "./packages/scripts/dev/setup_env.sh",
    "setup": "pnpm run reset && docker compose -f ./setup-compose.yaml -p setup up -d",
    "reset": "docker compose -p setup down && docker compose -p homelab down",
    "start": "pnpm run setup && docker compose -p homelab up",
    "build": "docker compose build",
    "dev": "sst dev"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "sst": "^3.9.10",
    "typescript": "5.7.3"
  }
}
