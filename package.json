{
  "name": "homelab-template",
  "version": "0.0.0",
  "packageManager": "pnpm@10.5.2",
  "scripts": {
    "setup": "pnpm run reset && docker compose -f ./setup-compose.yaml -p setup up -d",
    "reset": "docker compose -p setup down && docker compose -p homelab down",
    "build": "docker compose -f ./build-compose.yaml build",
    "k3d:push": "pnpm run build && pnpm -r run k3d:push",
    "dev": "sst dev"
  },
  "devDependencies": {
    "dotenv": "^16.4.7",
    "sst": "^3.9.10",
    "typescript": "5.7.3"
  }
}
