apiVersion: apps/v1
kind: Deployment
metadata:
  name: sveltekit-web-deployment
  namespace: main
spec:
  template:
    spec:
      containers:
        - name: web
          envFrom:
            - secretRef:
                name: env

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pgbackrest-backup
  namespace: main
spec:
  template:
    spec:
      containers:
        - name: pg-backup
          envFrom:
            - secretRef:
                name: env

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pgcat-pooler
  namespace: main
spec:
  template:
    spec:
      containers:
        - name: pgcat
          envFrom:
            - secretRef:
                name: env
