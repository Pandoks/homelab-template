apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: masterdb-main-1
  namespace: main
  labels:
    app: masterdb-main-1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: masterdb-main-1
  template:
    metadata:
      labels:
        app: masterdb-main-1
    spec:
      securityContext:
        fsGroup: 1000
      containers:
        - name: masterdb-main-1
          image: local-registry:12345/masterdb:latest
          env:
            - name: POSTGRES_DB
              value: main
            - name: PGDATABASE
              value: main
            - name: POSTGRES_TYPE
              value: backup
            - name: MASTER_HOST
              value: masterdb-1
          volumeMounts:
            - name: masterdb-main-config
              mountPath: /docker-entrypoint-initdb.d/0000_init.sql
              subPath: 0000_init.sql
            - name: masterdb-main-config
              mountPath: /docker-entrypoint-initdb.d/0001_roles.sql
              subPath: 0001_roles.sql
            - name: masterdb-main-config
              mountPath: /docker-entrypoint-initdb.d/0002_extensions.sql
              subPath: 0002_extensions.sql
            - name: masterdb-main-config
              mountPath: /docker-entrypoint-initdb.d/0003_cron.sql
              subPath: 0003_cron.sql
            - name: masterdb-main-config
              mountPath: /docker-entrypoint-initdb.d/master.sh
              subPath: master.sh
            - name: postgres-main-config
              mountPath: /tmp/conf_templates/postgresql.conf
              subPath: postgresql.conf
            - name: postgres-main-config
              mountPath: /tmp/conf_templates/pg_hba.conf
              subPath: pg_hba.conf
            - name: masterdb-main-config
              mountPath: /tmp/conf_templates/pgbackrest.conf
              subPath: pgbackrest.conf
            - name: masterdb-ca-cert
              mountPath: /etc/pgbackrest/ca.crt
              subPath: tls.crt
            - name: pgbackrest-server-tls
              mountPath: /etc/pgbackrest/server.crt
              subPath: tls.crt
            - name: pgbackrest-server-tls
              mountPath: /etc/pgbackrest/server.key
              subPath: tls.key
            - name: masterdb-client-tls
              mountPath: /etc/pgbackrest/client.crt
              subPath: tls.crt
            - name: masterdb-client-tls
              mountPath: /etc/pgbackrest/client.key
              subPath: tls.key
      volumes:
        - name: masterdb-main-config
          configMap:
            name: masterdb-main-config
        - name: postgres-main-config
          configMap:
            name: postgres-main-config
        - name: masterdb-ca-cert
          secret:
            secretName: internal-root-ca-secret
            defaultMode: 0600
        - name: pgbackrest-server-tls
          secret:
            secretName: pgbackrest-backup-tls
            defaultMode: 0600
        - name: masterdb-client-tls
          secret:
            secretName: masterdb-main-1-tls
            defaultMode: 0600
  volumeClaimTemplates:
    - metadata:
        name: masterdb-main-1-data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: local-path
        resources:
          requests:
            storage: 1Gi
